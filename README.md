# MusicLibrary
A RESTful Music Library API built using Spring Boot that allows users to manage songs with CRUD operations (Create, Read, Update, Delete). The project demonstrates backend development concepts including REST APIs, controller-service architecture, and data handling using Java.
